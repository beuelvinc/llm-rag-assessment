services:
  worker:
    image: my-worker-image:latest
    command: ["./run.sh",  "run_all_tests"]
    volumes:
      - .:/app
    extra_hosts:
      - "host.docker.internal:host-gateway"
    stdin_open: true
    tty: true
#    deploy:
#      resources:
#        limits:
#          memory: 10g
